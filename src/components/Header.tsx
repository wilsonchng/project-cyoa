const Header = () => {
    return (
        <div className="header">
            {/* Link to main menu */}
            {/* Link to character stats */}
        </div>
    );
};

export default Header;
